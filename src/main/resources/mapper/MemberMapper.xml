<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.soft.shopping.mapper.MemberMapper">



    <insert id="register" parameterType="MemberRegisterDTO">
        INSERT INTO STUDY_USERS ( USER_ID, USER_PW, USER_NICKNAME )
        VALUES (#{userId}, #{userPw}, #{userNickname})
    </insert>


    <select id="login" parameterType="String" resultType="String">
        SELECT USER_PW FROM STUDY_USERS
        WHERE USER_ID = #{userId}
    </select>


    <select id="userInfo" parameterType="String" resultType="Integer">
        SELECT USER_IDX FROM STUDY_USERS
        WHERE USER_ID = #{userId}
    </select>


</mapper>
